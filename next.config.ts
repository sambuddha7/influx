import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'assets.aceternity.com', 'media.gettyimages.com'],
  },
};

export default nextConfig;
